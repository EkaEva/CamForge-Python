中文 | [English](README.en.md) | [日本語](README.ja.md)

<div align="center">

# CamForge

**尖顶直动从动件盘形凸轮机构运动学仿真系统**

[![Python](https://img.shields.io/badge/Python-3.10%2B-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20macOS%20%7C%20Linux-lightgrey.svg)]()

</div>

---

## 项目简介

CamForge 是一款基于 Python 的凸轮机构运动学仿真桌面应用，面向机械工程教学与设计场景。支持五种经典从动件运动规律、凸轮廓形解析计算、压力角实时可视化以及全程旋转动画，帮助工程师和学生直观理解凸轮机构的运动学特性。

### 核心亮点

- **五种运动规律** — 等速、等加速等减速、简谐、摆线、五次多项式，推程与回程可独立选择
- **解析计算** — 廓形坐标与压力角均采用解析公式，非数值微分，精度高、无跳动
- **实时动画** — 360 帧旋转动画，支持速度调节、暂停/继续，同步显示压力角弧线与法线/切线
- **一键导出** — 静态图表导出为高清 PNG，动画导出为循环 GIF
- **随机参数** — 一键生成合法随机凸轮参数，快速探索不同构型
- **参数校验** — 自动检测四角之和、偏距范围等约束，异常参数即时提示

---

## 功能展示

### 界面布局

```
┌──────────────────────────────────────────────────────────────┐
│  CamForge - 凸轮仿真                                          │
├────────────┬─────────────────────────────────────────────────┤
│            │  ┌─────────────┐  ┌─────────────┐              │
│  参数面板   │  │  位移曲线 s  │  │  速度曲线 v  │              │
│  ────────  │  └─────────────┘  └─────────────┘              │
│  推程角     │  ┌─────────────┐  ┌─────────────┐              │
│  远休止角   │  │  加速度曲线 a │  │  凸轮廓形    │              │
│  回程角     │  └─────────────┘  └─────────────┘              │
│  近休止角   │  ┌───────────────────────────────┐              │
│  行程/基圆  │  │                               │              │
│  偏距/角速度│  │      凸轮旋转动画 + 信息面板     │              │
│  运动规律   │  │                               │              │
│  旋向/偏距  │  └───────────────────────────────┘              │
│            │                                                 │
├────────────┴─────────────────────────────────────────────────┤
│  [▶ 开始]  [⏸ 暂停]  [清空参数]  [清空图表]  [随机凸轮]       │
│  [下载图片]  速度: ━━━━━●━━━━━  ☑位移 ☑速度 ☑加速度 ☑动画     │
└──────────────────────────────────────────────────────────────┘
```

### 运动规律一览

| 编号 | 运动规律 | 推程位移公式 | 特性 |
|:----:|---------|------------|------|
| 1 | 等速运动 | $s = h \cdot \dfrac{\delta}{\delta_0}$ | 速度恒定，行程始末有刚性冲击 |
| 2 | 等加速等减速 | $s = 2h\left(\dfrac{\delta}{\delta_0}\right)^2$（前半段） | 加速度恒定，行程中点有柔性冲击 |
| 3 | 简谐运动 | $s = \dfrac{h}{2}\left(1 - \cos\dfrac{\pi\delta}{\delta_0}\right)$ | 无刚性冲击，行程始末有柔性冲击 |
| 4 | 摆线运动 | $s = h\left(\dfrac{\delta}{\delta_0} - \dfrac{1}{2\pi}\sin\dfrac{2\pi\delta}{\delta_0}\right)$ | 无刚性冲击和柔性冲击，动力性能好 |
| 5 | 五次多项式 | $s = h\left(10t^3 - 15t^4 + 6t^5\right)$ | 高阶平滑，综合性能最优 |

### 动画特性

- **反转法**：凸轮旋转，从动件固定，直观展示实际运动
- **压力角弧线**：在接触点实时绘制压力角弧线与标注
- **法线/切线**：解析计算廓形导数方向，精确绘制接触点法线与切线
- **相位分界线**：径向标注推程/远休止/回程/近休止四个阶段
- **信息面板**：实时显示当前转角、压力角、位移、行程、初始位移

---

## 快速开始

### 环境要求

- Python 3.10 及以上
- Windows / macOS / Linux

### 安装依赖

```bash
pip install numpy>=1.20 matplotlib>=3.5 Pillow
```

> **注意**：`Pillow` 用于 GIF 动画导出，未包含在 `requirements.txt` 中，需手动安装。

### 启动应用

```bash
cd CamForge
python main.py
```

---

## 使用指南

### 1. 参数输入

在左侧面板中依次填入凸轮设计参数：

| 参数 | 符号 | 说明 | 约束 |
|-----|:----:|------|------|
| 推程运动角 | $\delta_0$ | 推程阶段凸轮转角 | 整数，> 1° |
| 远休止角 | $\delta_{01}$ | 从动件停留在最远位置 | 整数，> 1° |
| 回程运动角 | $\delta_{0}'$ | 回程阶段凸轮转角 | 整数，> 1° |
| 近休止角 | $\delta_{02}$ | 从动件停留在最近位置 | 整数，> 1° |
| 行程 | $h$ | 从动件最大位移 | > 0 mm |
| 基圆半径 | $r_0$ | 凸轮基圆半径 | > 0 mm |
| 偏距 | $e$ | 从动件导路偏移量 | < $r_0$ mm |
| 角速度 | $\omega$ | 凸轮旋转角速度 | > 0 rad/s |

> 四个角度之和必须恰好等于 **360°**。

### 2. 选择运动规律

- **推程规律**：下拉选择 1-5 号运动规律
- **回程规律**：下拉选择 1-5 号运动规律
- **旋向**：顺时针 / 逆时针
- **偏距方向**：正偏距 / 负偏距

### 3. 运行仿真

1. 点击 **「开始」** 按钮，计算并绘制静态曲线与凸轮廓形
2. 动画自动开始播放，展示凸轮旋转过程
3. 使用 **「暂停」** 按钮暂停/继续动画
4. 拖动 **速度滑块** 调节动画帧率（1 = 最慢，10 = 最快）

### 4. 导出结果

- 勾选需要导出的图表类型（位移 / 速度 / 加速度 / 动画）
- 点击 **「下载图片」** 按钮
- 静态图表保存为 **PNG**（150 DPI），动画保存为 **GIF**（循环播放）

### 5. 随机探索

点击 **「随机凸轮」** 按钮，自动生成一组满足约束的随机参数，快速体验不同凸轮构型。

---

## 技术架构

```
CamForge/
├── main.py              # GUI 应用入口（tkinter + matplotlib）
│   ├── CamSimulator     # 主窗口类
│   │   ├── _build_gui()       # 构建界面布局
│   │   ├── _on_start()        # 启动仿真计算
│   │   ├── _animate()         # 动画循环
│   │   ├── _on_download()     # 导出图片/GIF
│   │   └── ...                # 事件处理与绘图
│   └── generate_random_params()  # 随机参数生成器
│
├── cam_mechanics.py     # 运动学计算引擎（纯数学，无 GUI 依赖）
│   ├── compute_rise()          # 推程运动规律计算
│   ├── compute_return()        # 回程运动规律计算
│   ├── compute_full_motion()   # 全程运动合成
│   ├── compute_cam_profile()   # 凸轮廓形坐标计算
│   ├── compute_pressure_angle()# 压力角解析计算
│   ├── compute_rotated_cam()   # 凸轮旋转（动画帧）
│   ├── compute_anim_frame_data()# 动画帧数据解析计算
│   ├── compute_pressure_angle_arc()# 压力角弧线坐标
│   └── validate_params()       # 参数合法性校验
│
└── requirements.txt     # Python 依赖声明
```

### 设计原则

- **计算与展示分离**：`cam_mechanics.py` 为纯数学库，不依赖任何 GUI 框架，可独立用于批量计算或集成到其他项目
- **解析优先**：所有导数与压力角均采用解析公式（`ds/dδ = v/ω`），避免数值微分带来的精度损失
- **1° 步长离散**：全程 360 个离散点，每个整数角度对应一个计算点，兼顾精度与性能

---

## 核心算法

### 凸轮廓形计算（反转法）

廓形坐标由反转法解析求得：

$$
x = -s_n \left[ (s_0 + s)\sin\delta + p_z \cdot e\cos\delta \right]
$$

$$
y = (s_0 + s)\cos\delta - p_z \cdot e\sin\delta
$$

其中 $s_0 = \sqrt{r_0^2 - e^2}$，$s_n = \pm 1$ 为旋向符号，$p_z = \pm 1$ 为偏距符号。

### 压力角计算

$$
\alpha = \arctan\!\left(\frac{ds/d\delta - p_z \cdot e}{s_0 + s}\right)
$$

当最大压力角超过 **30°** 时，界面将显示红色警告提示。

---

## 依赖说明

| 依赖 | 版本要求 | 用途 |
|-----|---------|------|
| [numpy](https://numpy.org/) | >= 1.20 | 数值计算（数组运算、三角函数） |
| [matplotlib](https://matplotlib.org/) | >= 3.5 | 图表绘制与 tkinter 嵌入 |
| [Pillow](https://python-pillow.org/) | 任意 | GIF 动画导出 |
| tkinter | 标准库 | GUI 框架（Python 自带） |

---

## 常见问题

<details>
<summary><b>启动后界面中文显示为方块</b></summary>

本程序使用 SimHei / Microsoft YaHei 字体渲染中文。若系统缺少这些字体：
- **Windows**：一般自带，无需处理
- **macOS**：安装 SimHei 字体或修改 `main.py` 中的 `plt.rcParams['font.sans-serif']` 为系统已有中文字体
- **Linux**：`sudo apt install fonts-wqy-microhei` 并修改字体配置

</details>

<details>
<summary><b>动画播放卡顿</b></summary>

- 降低速度滑块档位
- 关闭不需要的显示选项（法线/切线、网格等）
- 动画默认每 2 帧刷新一次画布以减少延迟

</details>

<details>
<summary><b>GIF 导出失败</b></summary>

确保已安装 Pillow：`pip install Pillow`。GIF 导出需要逐帧渲染 360 帧图像，耗时较长，请耐心等待进度提示。

</details>

---

## 许可证

本项目采用 [MIT License](LICENSE) 开源协议。

---

<div align="center">

**CamForge** — 让凸轮设计直观而精确

</div>
