[中文](README.md) | [English](README.en.md) | 日本語

<div align="center">

# CamForge

**尖頂直動従動件盤形カム機構運動学シミュレーションシステム**

[![Python](https://img.shields.io/badge/Python-3.10%2B-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20macOS%20%7C%20Linux-lightgrey.svg)]()

</div>

---

## プロジェクト概要

CamForge は、Python ベースのカム機構運動学シミュレーションデスクトップアプリケーションで、機械工学の教育と設計シーンに向けられています。5 つの古典的な従動件運動法則、カム輪郭の解析計算、圧力角のリアルタイム可視化、全行程回転アニメーションをサポートし、エンジニアと学生がカム機構の運動学特性を直感的に理解できるよう支援します。

### 主な特徴

- **5 種類の運動法則** — 等速、等加速等減速、単振動、サイクロイド、5 次多項式。上昇行程と戻り行程を独立して選択可能
- **解析計算** — 輪郭座標と圧力角はすべて解析公式を採用。数値微分ではなく、精度が高く跳動なし
- **リアルタイムアニメーション** — 360 フレーム回転アニメーション、速度調整・一時停止/再開/再生対応、圧力角弧線と法線/接線の同期表示
- **多言語インターフェース** — 中国語・英語・日本語の3言語リアルタイム切替に対応。エクスポートファイル名も言語に応じて自動変更
- **ワンクリックエクスポート** — 静止図表は 600 DPI 高解像度 TIFF、アニメーションは 150 DPI ループ GIF、データは Excel 表でエクスポート
- **ランダムパラメータ** — ワンクリックで正当なランダムカムパラメータを生成、異なる構成を素早く探索
- **パラメータ検証** — 四角の和、オフセット範囲などの制約を自動検出、異常パラメータを即時通知
- **キーボードショートカット** — Enter でシミュレーション開始、Space で一時停止/再開/再生、R でランダムカム

---

## 機能紹介

### インターフェースレイアウト

```
┌──────────────────────────────────────────────────────────────┐
│  CamForge - カムシミュレーション                                │
├────────────┬─────────────────────────────────────────────────┤
│            │  ┌─────────────┐  ┌─────────────┐              │
│  言語      │  │  変位曲線 s  │  │  速度曲線 v  │              │
│  ────────  │  └─────────────┘  └─────────────┘              │
│  運動      │  ┌─────────────┐  ┌─────────────┐              │
│  パラメータ │  │  加速度曲線 a │  │  カム輪郭    │              │
│  上昇角     │  └─────────────┘  └─────────────┘              │
│  遠休止角   │  ┌───────────────────────────────┐              │
│  戻り角     │  │                               │              │
│  近休止角   │  │                               │              │
│  行程/基礎円│  │  カム回転アニメーション + 情報パネル │              │
│  オフセット │  │                               │              │
│  /角速度    │  │                               │              │
│  運動法則   │  └───────────────────────────────┘              │
│  回転方向   │                                                 │
│  /オフセット│                                                 │
│  ────────  │                                                 │
│  表示オプション│                                               │
│  ☐接線     │                                                 │
│  ☐法線     │                                                 │
│  ☐圧力角弧 │                                                 │
│  ☐位相境界 │                                                 │
│  ☐基礎円   │                                                 │
│  ☐オフセット円│                                                │
│  ☐上下限   │                                                 │
│  ☐グリッド │                                                 │
├────────────┴─────────────────────────────────────────────────┤
│  [▶ 開始]  [⏸ 一時停止]  [パラメータクリア]  [図表クリア]       │
│  [ランダムカム]  [ダウンロード]  速度: ━━━━━●━━━━━             │
│  ☑変位 ☑速度 ☑加速度 ☑輪郭 ☑アニメ ☑Excel                    │
└──────────────────────────────────────────────────────────────┘
```

### 運動法則一覧

| 番号 | 運動法則 | 上昇行程変位公式 | 特性 |
|:----:|---------|------------|------|
| 1 | 等速運動 | $s = h \cdot \dfrac{\delta}{\delta_0}$ | 速度一定、行程の始端と終端で剛性衝撃あり |
| 2 | 等加速等減速 | $s = 2h\left(\dfrac{\delta}{\delta_0}\right)^2$（前半区間） | 加速度一定、行程中点で柔衝撃あり |
| 3 | 単振動 | $s = \dfrac{h}{2}\left(1 - \cos\dfrac{\pi\delta}{\delta_0}\right)$ | 剛性衝撃なし、行程の始端と終端で柔衝撃あり |
| 4 | サイクロイド運動 | $s = h\left(\dfrac{\delta}{\delta_0} - \dfrac{1}{2\pi}\sin\dfrac{2\pi\delta}{\delta_0}\right)$ | 剛性衝撃・柔衝撃なし、動力性能良好 |
| 5 | 5 次多項式 | $s = h\left(10t^3 - 15t^4 + 6t^5\right)$ | 高次平滑、総合性能最優 |

### アニメーション特性

- **反転法**：カムが回転し、従動件は固定、実際の運動を直感的に表示
- **固定ヒンジ支持**：回転中心に標準的な固定ヒンジ支持記号（ピン円 + 三角形 + ハッチ底座）を描画
- **圧力角弧線**：接触点に圧力角弧線と注釈をリアルタイム描画
- **法線/接線**：輪郭導関数方向を解析計算し、接触点の法線と接線を正確に描画
- **位相区切り線**：放射状に上昇/遠休止/戻り/近休止の 4 段階を標示
- **情報パネル**：現在の回転角、圧力角、変位、行程、初期変位をリアルタイム表示
- **設定可能な表示**：8 つの表示オプション（接線、法線、圧力角弧線、位相境界線、基礎円、オフセット円、フォロア上下限、グリッド）。エクスポートGIFは画面表示と完全に一致

---

## クイックスタート

### 動作環境

- Python 3.10 以上
- Windows / macOS / Linux

### 依存パッケージのインストール

```bash
pip install -r requirements.txt
```

### アプリケーションの起動

```bash
cd CamForge
python main.py
```

---

## 使用ガイド

### 1. 言語切替

サイドバー上部で言語を選択（中文 / English / 日本語）。すべてのラベル、ボタン、グラフタイトル、ステータスメッセージ、エクスポートファイル名が即座に切替わります。

### 2. パラメータ入力

左側パネルにカム設計パラメータを順に入力します：

| パラメータ | 記号 | 説明 | 制約 |
|-----|:----:|------|------|
| 上昇運動角 | $\delta_0$ | 上昇行程におけるカム回転角 | 整数、> 1° |
| 遠休止角 | $\delta_{01}$ | 従動件が最遠位置に留まる角度 | 整数、> 1° |
| 戻り運動角 | $\delta_{0}'$ | 戻り行程におけるカム回転角 | 整数、> 1° |
| 近休止角 | $\delta_{02}$ | 従動件が最近位置に留まる角度 | 整数、> 1° |
| 行程 | $h$ | 従動件の最大変位 | > 0 mm |
| 基礎円半径 | $r_0$ | カム基礎円半径 | > 0 mm |
| オフセット | $e$ | 従動件ガイドの偏り量 | < $r_0$ mm |
| 角速度 | $\omega$ | カム回転角速度 | > 0 rad/s |

> 4 つの角度の和は正確に **360°** でなければなりません。

### 3. 運動法則の選択

- **上昇行程法則**：ドロップダウンから 1〜5 号の運動法則を選択
- **戻り行程法則**：ドロップダウンから 1〜5 号の運動法則を選択
- **回転方向**：時計回り / 反時計回り
- **オフセット方向**：正オフセット / 負オフセット

### 4. シミュレーションの実行

1. **「開始」** ボタンをクリック（または Enter キー）し、静止曲線とカム輪郭を計算・描画
2. アニメーションが自動的に再生され、カム回転過程を表示
3. **「一時停止」** ボタンでアニメーションの一時停止/再開（または Space キー）
4. アニメーション終了後、Space キーまたは **「再生」** ボタンで再再生
5. **速度スライダー** をドラッグしてアニメーションフレームレートを調整（1 = 最遅、10 = 最速）

### 5. 表示オプション

サイドバーの「表示オプション」セクションには8つのチェックボックスがあり、アニメーションビューの補助要素の表示を制御します：

| オプション | 説明 | デフォルト |
|-----------|------|:---------:|
| 接線 | 接触点の接線 | オフ |
| 法線 | 接触点の法線 | オフ |
| 圧力角弧線 | 圧力角弧線と参照線 | オフ |
| 位相境界線 | 放射状位相境界線 | オフ |
| 基礎円 | 基礎円輪郭 | オフ |
| オフセット円 | オフセット円輪郭 | オフ |
| フォロア上下限 | 上下限水平線 | オフ |
| グリッド | 座標グリッド | オフ |

> GIFエクスポート時、表示オプションは画面表示と完全に一致します。チェックされた要素のみがエクスポート画像に含まれます。

### 6. 結果のエクスポート

エクスポートする項目にチェックを入れ、**「ダウンロード」** ボタンをクリック：

| タイプ | フォーマット | 説明 |
|--------|-------------|------|
| 変位曲線 | TIFF (600 DPI) | フォロア変位の角度変化 |
| 速度曲線 | TIFF (600 DPI) | フォロア速度の角度変化 |
| 加速度曲線 | TIFF (600 DPI) | フォロア加速度の角度変化 |
| カム輪郭 | TIFF (600 DPI) | カム輪郭 + 基礎円 + オフセット円 + 固定ヒンジ支持 |
| アニメーション | GIF (150 DPI) | 360フレームループアニメーション（進捗表示付き） |
| Excel | XLSX | 回転角 δ、動径 R、速度 v、加速度 a |

### 7. ランダム探索

**「ランダムカム」** ボタンをクリック（または R キー）すると、制約を満たすランダムパラメータが自動生成され、異なるカム構成を素早く体験できます。

---

## 技術アーキテクチャ

```
CamForge/
├── main.py              # GUI アプリケーションエントリ（tkinter + matplotlib）
│   ├── CamSimulator     # メインウィンドウクラス
│   │   ├── _build_gui()       # インターフェースレイアウト構築
│   │   ├── _on_start()        # シミュレーション計算の開始
│   │   ├── _animate_frame()   # アニメーションフレームレンダリング
│   │   ├── _on_download()     # 画像/GIF/Excel エクスポート
│   │   ├── _export_gif()      # GIF バックグラウンドスレッドエクスポート
│   │   ├── _export_excel()    # Excel データエクスポート
│   │   └── ...                # イベント処理、i18n、描画
│   ├── draw_fixed_support()   # 固定ヒンジ支持記号描画
│   └── generate_random_params()  # ランダムパラメータ生成器
│
├── cam_mechanics.py     # 運動学計算エンジン（純粋数学、GUI 依存なし）
│   ├── compute_rise()          # 上昇行程運動法則計算
│   ├── compute_return()        # 戻り行程運動法則計算
│   ├── compute_full_motion()   # 全行程運動合成
│   ├── compute_cam_profile()   # カム輪郭座標計算
│   ├── compute_pressure_angle()# 圧力角解析計算
│   ├── compute_rotated_cam()   # カム回転（アニメーションフレーム）
│   ├── compute_anim_frame_data()# アニメーションフレームデータ解析計算
│   ├── compute_pressure_angle_arc()# 圧力角弧線座標
│   └── validate_params()       # パラメータ妥当性検証
│
├── i18n.py               # 国際化モジュール（zh/en/ja）
│   ├── TRANSLATIONS      # 86 項の翻訳辞書
│   ├── FONT_MAP          # クロスプラットフォームフォントマッピング
│   └── t()               # 翻訳検索関数
│
├── tests/                # 単体テスト（pytest、59 項すべて合格）
│   └── test_cam_mechanics.py
│
├── requirements.txt      # Python 依存関係宣言
├── LICENSE               # MIT ライセンス
└── .gitignore            # Git 無視ルール
```

### 設計原則

- **計算と表示の分離**：`cam_mechanics.py` は純粋な数学ライブラリであり、GUI フレームワークに依存しないため、単独でバッチ計算や他プロジェクトへの統合が可能
- **解析優先**：すべての導関数と圧力角は解析公式（`ds/dδ = v/ω`）を採用し、数値微分による精度損失を回避
- **1° ステップ離散化**：全行程 360 個の離散点、各整数角度に対応する計算点を持ち、精度と性能を両立
- **国際化設計**：すべての UI 文字列は i18n キー検索で解決。実行時の言語切替に再起動は不要

---

## コアアルゴリズム

### カム輪郭計算（反転法）

輪郭座標は反転法により解析的に求められます：

$$
x = -s_n \left[ (s_0 + s)\sin\delta + p_z \cdot e\cos\delta \right]
$$

$$
y = (s_0 + s)\cos\delta - p_z \cdot e\sin\delta
$$

ここで $s_0 = \sqrt{r_0^2 - e^2}$、$s_n = \pm 1$ は回転方向符号、$p_z = \pm 1$ はオフセット符号です。

### 圧力角計算

$$
\alpha = \arctan\!\left(\frac{ds/d\delta - p_z \cdot e}{s_0 + s}\right)
$$

最大圧力角が **30°** を超える場合、インターフェースに赤色の警告が表示されます。

---

## 依存関係

| 依存パッケージ | バージョン要件 | 用途 |
|-----|---------|------|
| [numpy](https://numpy.org/) | >= 1.20 | 数値計算（配列演算、三角関数） |
| [matplotlib](https://matplotlib.org/) | >= 3.5 | 図表描画と tkinter 埋め込み |
| [Pillow](https://python-pillow.org/) | >= 9.0 | GIF アニメーションのエクスポート |
| [openpyxl](https://openpyxl.readthedocs.io/) | >= 3.0 | Excel データエクスポート |
| tkinter | 標準ライブラリ | GUI フレームワーク（Python 同梱） |

---

## よくある質問

<details>
<summary><b>起動後にインターフェースの中国語が四角で表示される</b></summary>

本プログラムは利用可能なシステムフォントを自動検出し、フォールバックします。問題が続く場合：
- **Windows**：通常はプリインストール済み、対応不要
- **macOS**：アプリが Yu Gothic / Hiragino Sans 等を自動検出
- **Linux**：`sudo apt install fonts-wqy-microhei` を実行し、フォント設定を変更

</details>

<details>
<summary><b>アニメーション再生がカクつく</b></summary>

- 速度スライダーのレベルを下げる
- 不要な表示オプション（法線/接線、グリッドなど）をオフにする
- アニメーションは遅延を減らすため、デフォルトで 2 フレームごとにキャンバスを更新

</details>

<details>
<summary><b>GIF エクスポートに時間がかかる</b></summary>

GIF エクスポートでは 360 フレームの画像を逐次レンダリングします。バックグラウンドスレッドとプログレスバーで UI 凍結を回避します。フレームレンダリング完了後、GIF 合成が行われます。プログレスラベルに「GIF合成中...」と表示されます。しばらくお待ちください。

</details>

---

## ライセンス

本项目は [MIT License](LICENSE) オープンソースライセンスを採用しています。

---

<div align="center">

**CamForge** — カム設計を直感的かつ精密に

</div>
