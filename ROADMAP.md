中文 | [English](ROADMAP.en.md) | [日本語](ROADMAP.ja.md)

# CamForge Roadmap

> 项目现状分析与后续优化方向

---

## 一、现存问题

### 1.1 代码架构

| # | 问题 | 位置 | 严重程度 |
|---|------|------|:--------:|
| 1 | `CamSimulator` 类超过 **1390 行**，GUI 构建、计算、动画、导出、i18n 逻辑全部耦合 | `main.py` | 高 |
| 2 | `_build_gui` 方法约 **300 行**，侧边栏/工具栏/图表布局全部内联 | `main.py:218-522` | 高 |
| 3 | 静态绘图代码在 `_plot_static` 和 `_on_download` 中重复 | `main.py:698-782` vs `1116-1214` | 中 |
| 4 | 推杆绘制逻辑在 `_animate_frame` 和 GIF 导出中仍有重复 | `main.py:1000-1010` vs `1368-1371` | 中 |
| 5 | 颜色值散布为字符串字面量（`'#f8fafc'`、`'#2563eb'` 等），未提取为主题常量 | `main.py` 多处 | 中 |
| 6 | 标准库模块在方法内部 `import`（`os`、`threading`、`BytesIO`、`filedialog`） | `main.py:1093-1095` 等 | 低 |

### 1.2 健壮性

| # | 问题 | 位置 | 严重程度 |
|---|------|------|:--------:|
| 7 | GIF 导出线程读取 `sim_data` 无锁保护，用户中途重新仿真可能导致数据不一致 | `main.py:1332` | 中 |
| 8 | `_export_excel` 的 `wb.save()` 无 try/except，磁盘满或路径无效时崩溃 | `main.py:1280` | 中 |
| 9 | `_on_download` 中各 `fig.savefig()` 无错误处理 | `main.py:1116-1214` | 中 |
| 10 | 键盘快捷键 `R` 全局绑定，在输入框中无法输入字母 r | `main.py:145` | 中 |
| 11 | 状态栏警告覆盖问题：压力角警告和行程警告同时触发时只显示后者 | `main.py:666-672` | 中 |
| 12 | `_on_close` 不等待 GIF 导出线程完成，可能中断导出 | `main.py:1488-1491` | 低 |
| 13 | `generate_random_params` 无迭代安全上限 | `main.py:80-86` | 低 |

### 1.3 计算引擎

| # | 问题 | 位置 | 严重程度 |
|---|------|------|:--------:|
| 14 | `compute_rise`/`compute_return` 中 `delta_0=0` 或 `omega=0` 时除零，函数内部无防御 | `cam_mechanics.py` | 中 |
| 15 | `compute_cam_profile` 中 `e > r_0` 时 `sqrt` 产生 NaN，无保护性检查 | `cam_mechanics.py:252` | 中 |
| 16 | `compute_pressure_angle` 中 `s_0 + s = 0` 时除零（浮点精度下 `e ≈ r_0`） | `cam_mechanics.py:286` | 中 |
| 17 | 等加速等减速规律（law 2）在 `delta_0` 为奇数时中点不对齐离散网格 | `cam_mechanics.py:37-55` | 低 |
| 18 | `N_POINTS = 360` 硬编码，无法调整分辨率 | `cam_mechanics.py:8` | 低 |
| 19 | 压力角弧线绘制阈值 `0.5°` 硬编码 | `cam_mechanics.py:425` | 低 |

### 1.3 国际化

| # | 问题 | 位置 | 严重程度 |
|---|------|------|:--------:|
| 20 | 静态图标题使用中文括号 `（）`，英文/日文下应使用 `()` | `main.py:720` | 低 |
| 21 | Excel 工作表名 "CamForge" 硬编码，未走 i18n | `main.py:1250` | 低 |
| 22 | openpyxl 缺失错误提示硬编码为英文 | `main.py:1237` | 低 |
| 23 | 运动规律下拉列表中文"规律"后缀不一致（1/2 无，3/4/5 有） | `i18n.py:law.combo.*` | 低 |

### 1.4 测试与工程化

| # | 问题 | 位置 | 严重程度 |
|---|------|------|:--------:|
| 24 | 无 i18n 测试（键完整性、回退行为） | `tests/` | 中 |
| 25 | 无压力角推程/回程阶段测试 | `tests/` | 中 |
| 26 | 无 `sn=-1`/`pz=-1`（旋向/偏距方向）的廓形测试 | `tests/` | 中 |
| 27 | 无 GUI 集成测试 | `tests/` | 低 |
| 28 | 无 `pyproject.toml`、无 `conftest.py`、无 `pytest-cov` | 项目根目录 | 中 |
| 29 | `requirements.txt` 无上界约束，numpy 2.x 可能破坏兼容性 | `requirements.txt` | 中 |
| 30 | 无类型注解，无法进行静态检查 | 全部文件 | 低 |

---

## 二、优化方向

### Phase 1 — 修复与加固（v0.2）✅ 已完成

- [x] **P1-1** 补全 `requirements.txt`，添加 `Pillow` 依赖
- [x] **P1-2** 修复跨平台字体：运行时检测系统可用中文字体，自动回退
- [x] **P1-3** 修复跨平台滚轮事件：区分 Windows / macOS / Linux 的 `delta` 值与事件类型
- [x] **P1-4** 修复窗口最大化兼容性：按平台选择 `state('zoomed')` 或 `attributes('-fullscreen')`
- [x] **P1-5** 修复浮点校验问题：`_read_params` 中角度先转 `int` 再传入 `validate_params`
- [x] **P1-6** 添加 `LICENSE` 文件（MIT）
- [x] **P1-7** 完善 `.gitignore`（IDE 目录、虚拟环境、构建产物等）
- [x] **P1-8** 为 `cam_mechanics.py` 添加单元测试（pytest，59 项全部通过）

### Phase 2 — 重构与性能（v0.3）

目标：改善代码结构，提升动画性能与导出体验。

- [ ] **P2-1** 拆分 `CamSimulator` 为独立模块：
  ```
  ui/
  ├── sidebar.py      # 侧边栏构建与参数管理
  ├── toolbar.py      # 工具栏按钮与控件
  ├── plots.py        # 静态图表绘制
  ├── animation.py    # 动画控制与帧渲染
  ├── export.py       # 图片/GIF/Excel 导出
  └── app.py          # 主窗口组装与启动
  ```
- [x] **P2-2** 提取魔法数字为命名常量或配置项
- [x] **P2-3** GIF 导出改为后台线程 + 进度条，避免 UI 冻结
- [x] **P2-4** 消除动画与 GIF 导出间的推杆绘制重复代码，抽取为共享函数
- [x] **P2-5** 静态图导出改为直接重新计算绘图，消除 `BlendedGenericTransform` hack
- [x] **P2-6** 动画结束后支持「重播」按钮，无需重新计算
- [x] **P2-7** 下载文件名通过 i18n 键值动态生成，随语言切换
- [x] **P2-8** 提取颜色常量为主题字典，为深色模式做准备
- [x] **P2-9** 将方法内标准库 `import` 移至模块顶层
- [x] **P2-10** 消除 `_plot_static` 与 `_on_download` 间的静态绘图代码重复

### Phase 3 — 功能增强（v0.4）

目标：扩展仿真功能，提升工程实用性。

- [ ] **P3-1** 滚子从动件支持：增加滚子半径参数，计算实际廓形（等距曲线）与理论廓形
- [ ] **P3-2** 摆动从动件支持：支持摆动从动件盘形凸轮机构
- [ ] **P3-3** 压力角曲线图：新增压力角随转角变化的独立图表
- [ ] **P3-4** 凸轮轮廓曲率半径计算与显示，标注最小曲率半径位置
- [ ] **P3-5** 参数预设系统：保存/加载常用凸轮配置（JSON 文件）
- [x] **P3-6** 输入框默认值填充，降低首次使用门槛
- [x] **P3-7** 键盘快捷键：Enter 开始、Space 暂停/继续/重播、R 随机
- [x] **P3-8** 静态曲线图标题标注当前运动规律名称
- [ ] **P3-9** 压力角阈值可配置（侧边栏输入框或下拉选择）
- [x] **P3-10** 计算引擎防御性编程：`compute_*` 函数内部添加参数合法性断言
- [ ] **P3-11** `N_POINTS` 可配置化，支持更高或更低的离散分辨率

### Phase 4 — 体验升级（v0.5）

目标：打磨交互细节，提升视觉与操作体验。

- [ ] **P4-1** 深色模式支持（tkinter 主题 + matplotlib 样式切换）
- [ ] **P4-2** 图表交互增强：鼠标悬停显示数值、缩放/平移、点击定位到对应动画帧
- [ ] **P4-3** 动画帧进度条：显示当前帧/总帧数，支持拖动跳转
- [ ] **P4-4** 窗口缩放自适应：监听 `Configure` 事件动态调整 Figure 尺寸
- [ ] **P4-5** 参数输入实时校验：输入时即时提示范围错误，而非点击「开始」后才报错
- [x] **P4-6** 多语言支持（i18n）：中/英/日三语界面实时切换
- [ ] **P4-7** 导出格式扩展：支持 SVG 矢量图、CSV 数据表导出
- [ ] **P4-8** 凸轮 3D 可视化预览（可选，基于 matplotlib 3D 或 PyOpenGL）
- [x] **P4-9** 键盘快捷键焦点感知：输入框聚焦时屏蔽单字母快捷键
- [x] **P4-10** 状态栏多警告累积显示，避免覆盖
- [x] **P4-11** 「清除参数」改为恢复默认值而非留空
- [x] **P4-12** 修复 i18n 细节：中文括号→英文括号、Excel 表名/错误提示国际化

### Phase 5 — 工程化与发布（v1.0）

目标：达到可发布质量，支持便捷安装与分发。

- [ ] **P5-1** 添加 `pyproject.toml`，支持 `pip install camforge`
- [ ] **P5-2** GitHub Actions CI：自动运行测试、代码风格检查（ruff / black）
- [ ] **P5-3** PyPI 发布流程
- [ ] **P5-4** 打包为桌面应用（PyInstaller / Nuitka），提供 Windows / macOS 安装包
- [ ] **P5-5** 在线文档（GitHub Pages）：使用说明、算法原理、API 文档
- [ ] **P5-6** 变更日志（CHANGELOG.md）维护
- [ ] **P5-7** 代码覆盖率目标：`cam_mechanics.py` ≥ 90%，整体 ≥ 70%
- [ ] **P5-8** 添加类型注解，配置 mypy/pyright 静态检查
- [ ] **P5-9** `requirements.txt` 添加上界约束，防止 numpy 2.x 等破坏性升级
- [ ] **P5-10** 补全测试：i18n 键完整性、压力角推程/回程、旋向/偏距方向廓形

---

## 三、问题统计

```
严重程度分布：
  高  ████░░░░░░  2 项  （大类耦合、长方法）
  中  ████████████████░░  16 项  （代码重复、线程安全、错误处理、测试缺口等）
  低  ██████████░░░░░░  10 项  （i18n 细节、硬编码阈值、类型注解等）
```

---

## 四、优先级原则

1. **正确性优先** — 影响功能结果的问题（除零、线程安全、错误处理）优先于体验问题
2. **可测试性优先** — 为纯计算模块添加测试是后续所有重构的安全网
3. **渐进重构** — 拆分大类时保持功能不变，每步可独立验证
4. **用户感知优先** — 直接影响使用体验的问题（快捷键冲突、警告覆盖）尽早解决
5. **国际化一致性** — 所有用户可见字符串必须走 i18n，包括错误提示和格式细节
